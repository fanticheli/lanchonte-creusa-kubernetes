apiVersion: apps/v1
kind: Deployment
metadata:
  name: express-app-deployment-ms-pedido
spec:
  replicas: 2
  selector:
    matchLabels:
      app: express-app-ms-pedido
  template:
    metadata:
      labels:
        app: express-app-ms-pedido
    spec:
      containers:
        - name: express-container-ms-pedido
          image: acrlanchonete.azurecr.io/lanchonetecreusa-ms-pedido:{{.Values.imageTag}}
          ports:
            - containerPort: 3000
          env:
            - name: MONGO_URI
              valueFrom:
                configMapKeyRef:
                  name: lanchonete-configmap
                  key: MONGO_URI_PEDIDO
            - name: URL_MS_PAGAMENTO
              valueFrom:
                configMapKeyRef:
                  name: lanchonete-configmap
                  key: URL_MS_PAGAMENTO
            - name: URL_MS_PRODUCAO
              valueFrom:
                configMapKeyRef:
                  name: lanchonete-configmap
                  key: URL_MS_PRODUCAO
